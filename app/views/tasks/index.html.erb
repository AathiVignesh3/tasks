<p style="color: green"><%= notice %></p>

<h1>Tasks</h1>

<table id="tasks">
  <% @tasks.each do |task| %>
  <tr><td><%= render task %></td>
  <td><%= link_to "Show this task", task %></td>
  </tr>
  <% end %>
</table>

<%= link_to "New task", new_task_path %>
